{
  "name": "job-controller",
  "private": true,
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "check-types": "tsc --noEmit",
    "test": "bun test",
    "send": "bun scripts/send.ts",
    "queues": "bun scripts/queues.ts",
    "clear": "bun scripts/clear.ts"
  },
  "dependencies": {
    "@clickhouse/client": "^1.11.0",
    "@repo/queue": "*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
